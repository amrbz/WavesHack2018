cluster.name: chainify
# Crate Configuration
# network.host: _local_,_site_
# Paths
# path:
#   logs: /data/log
#   data: /data/data
# blobs:
#   path: /data/blobs
auth:
  trust:
    http_default_user: admin
  host_based:
    config:
      0:
        user: crate
        address: _local_
        method: trust
      99:
        method: password